
{
  "name": "backend",
  "version": "1.0.0",
  "description": "Local server to handle image uploads from Figma Plugin UI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "multer": "^1.4.4"
  }
}
